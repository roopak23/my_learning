name: new ci workflow
on: 
  push:
    branches: [ "master" ]
  workflow_dispatch:
  

jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
       - run: echo 'hello world'
       - run: echo $HOSTNAME
     
