SELECT
    userid,
    sum(automotive),
    sum(books_and_literature),
    sum(business_and_finance),
    sum(careers),
    sum(education),
    sum(events_and_attractions),
    sum(family_and_relationships),
    sum(fine_art),
    sum(food_and_drink),
    sum(healthy_living),
    sum(hobbies_and_interests),
    sum(home_and_garden),
    sum(medical_health),
    sum(movies),
    sum(music_and_audio),
    sum(news_and_politics),
    sum(personal_finance),
    sum(pets),
    sum(pop_culture),
    sum(real_estate),
    sum(religion_and_spirituality),
    sum(science),
    sum(shopping),
    sum(sports),
    sum(style_and_fashion),
    sum(tech_and_computing),
    sum(television),
    sum(travel),
    sum(video_gaming),
    sum(sensitive_topics)
 FROM CAR_Interest
 WHERE partition_date < {{ params.ENDDATE }} AND partition_date > ({{ params.ENDDATE }} - 28)
 GROUP BY userid