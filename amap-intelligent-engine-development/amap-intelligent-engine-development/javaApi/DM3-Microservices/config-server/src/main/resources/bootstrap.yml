management:
  endpoints:
    web:
      exposure:
        include: "*"

server:
  port: 8888

spring:
  application:
    name: "@spring.application.name@"
  profiles:
    active: "@spring.profiles.active@"
    include: "@spring.profiles.include@"